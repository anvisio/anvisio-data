# Build artifacts
node_modules/
dist/
*.tsbuildinfo

# CI runtime artifacts (regenerated each CI run)
ci-summary.md

# Editor / OS noise
.DS_Store
*.swp
*~

# Local-only experiments — name these so they're never committed
local/
scratch/
*.local.md
*.local.yaml
*.local.json
