_meta:
  name: manifests/salesforce/widgets.yaml
  version: 1.0.0
  cdn_schema_version: 60.4.2
  authored_by: cd2k + claude
  authored_at: 2026-05-16
  description: |
    Per-org Salesforce widgets stub. Inherits the full 13-widget set
    from widget-libraries/salesforce.widgets.yaml via the `extends:`
    field. Per-org overrides (custom field-layout selectors, vendor-
    specific custom components) would live under a `widgets:` map
    below; currently empty (the library is sufficient for the
    canonical SF Lightning UI).

    Promoted from .tmp-sf-manifest-8of8-2026-05-15/widgets.yaml on
    2026-05-16 — the original .tmp file authored `salesforce.text` /
    `salesforce.picklist` / etc. inline (12 widgets, ~240 lines). Now
    collapsed to a 2-line `extends:` stub since the library has the
    same widget ids canonically.

  changelog:
    - "1.0.0 (2026-05-16): initial promote from .tmp-sf-manifest-8of8-2026-05-15. Collapsed to extends-stub; library carries the 13-widget canonical set."

extends: widget-libraries/salesforce.widgets.yaml

# Per-app overrides go here. Empty = use extends: library as-is.
widgets: {}
