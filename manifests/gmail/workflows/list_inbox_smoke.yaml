_meta:
  name: manifests/gmail/workflows/list_inbox_smoke.yaml
  version: 0.1.0
  cdn_schema_version: 60.4.2
  authored_by: cd2k + claude
  authored_at: 2026-05-19
  description: |
    Single-step smoke: navigate to Gmail's Inbox folder and verify the
    thread-list landmark appears. Non-destructive (commits_changes: false
    on list_inbox). No inputs needed. The most minimal validation that
    the EXECUTE_V60_VERB handler + recipe runner can complete a full
    nav-and-verify cycle.

name: list_inbox_smoke
description: |
  End-to-end smoke for the gmail list_inbox action. Run via
  /demo-workflow gmail list_inbox_smoke. depends_on_view: any, no inputs
  — the cheapest possible workflow to validate the full pipeline.
steps:
  - id: open_inbox
    action: list_inbox
    flavor: dom_submit
    expected_outcome: navigated
