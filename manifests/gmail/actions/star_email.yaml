_meta:
  name: manifests/gmail/actions/star_email.yaml
  version: 0.3.0
  cdn_schema_version: 70.0.0
  authored_by: cd2k + claude
  authored_at: 2026-05-19
  description: |
    star_email atom — toggle the star flag on a thread. Spec 70:
    `browser` flavor clicks the row star icon; `mcp` flavor calls
    gmail.modify_email with addLabelIds: [STARRED].
  changelog:
    - "0.3.0 (2026-05-19): spec 70 shape — `dom` → `browser`, drop emits_when, add mcp_tool + mcp flavor."
    - "0.2.0 (2026-05-16): migrate to single `dom` flavor with commit marker (Phase 3)."
    - "0.1.0 (2026-05-10): initial seed from cd2k's healed gmail manifest."

description: >
  Star a thread (add the STARRED label). Browser flavor clicks the row's
  star icon. MCP flavor calls gmail.modify_email with addLabelIds: ["STARRED"].
  Both flavors are idempotent — callers ensure they're starring, not
  un-starring.
object: Thread
commits_changes: true
depends_on_view: Inbox
mcp_tool: gmail.modify_email
inputs:
  thread_id:
    type: text
    required: true
    description: data-legacy-thread-id of the row whose star is being toggled
result:
  toggled: boolean
flavors:
  browser:
    requires_auth: session_cookie
    label: Click star icon on row when commit:true
    recipe:
      - id: toggle_star
        type: use_widget
        commit: true
        args:
          widget: gmail.row_star_toggle
          op: invoke
          inputs:
            thread_id: "{{inputs.thread_id}}"
    outcomes:
      - action: ready_to_toggle_star
        signal:
          type: selector_appears
          selector: tr.zA:has(span[data-legacy-thread-id='{{inputs.thread_id}}'])
            span[role='button'].T-KT
      - action: toggled
        signal:
          type: selector_appears
          selector: tr.zA:has(span[data-legacy-thread-id='{{inputs.thread_id}}'])
            span[role='button']
        extract:
          toggled:
            literal: true
  mcp:
    requires_auth: mcp_server
    label: Star via gmail.modify_email MCP tool
    server: gmail
    tool: modify_email
    inputs_map:
      messageId: "{{inputs.thread_id}}"
      addLabelIds: ["STARRED"]
    outcomes:
      - action: toggled
        signal:
          type: network_response
          url_pattern: "*/mcp/*"
          status: 200
        extract:
          toggled:
            literal: true
