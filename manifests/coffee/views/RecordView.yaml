_meta:
  name: manifests/coffee/views/RecordView.yaml
  version: 0.1.0
  cdn_schema_version: 70.0.0
  authored_by: cd2k + claude (propose-observe)
  authored_at: 2026-07-09
  description: |
    The record detail drawer (Person Details, radix dialog).

name: RecordView
entry:
  url: "{instance.origin}/{object_segment}/{record_id}"
landmark_probe:
  selector: "[role=dialog]"
  timeout_ms: 8000
verify:
  - type: selector_appears
    selector: "[role=dialog]"
    timeout_ms: 8000
widgets: []
