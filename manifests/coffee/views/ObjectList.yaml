_meta:
  name: manifests/coffee/views/ObjectList.yaml
  version: 0.1.0
  cdn_schema_version: 70.0.0
  authored_by: cd2k + claude (propose-observe)
  authored_at: 2026-07-09
  description: |
    The object list view (search box + Add button + table).

name: ObjectList
entry:
  url: "{instance.origin}/{object_segment}"
landmark_probe:
  selector: "input[placeholder*='Search']"
  timeout_ms: 8000
verify:
  - type: selector_appears
    selector: "input[placeholder*='Search']"
    timeout_ms: 8000
widgets:
  - coffee.search_input
  - coffee.add_button
